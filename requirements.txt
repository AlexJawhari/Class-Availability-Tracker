# Core dependencies (required)
py-cord>=2.6.0
python-dotenv>=1.0.0
requests>=2.31.0
supabase>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
playwright>=1.40.0
playwright-stealth>=1.0.6

# Optional dependencies (fallback scrapers)
# curl_cffi is optional - if installation fails, Playwright and token extraction methods still work
# Note: curl_cffi requires Rust compiler during installation
curl_cffi>=0.6.0; platform_system != "Windows" or python_version >= "3.12"